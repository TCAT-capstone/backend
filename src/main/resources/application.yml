spring:
  datasource:
    url: jdbc:mysql://(host):(port이름)/mytcatdb?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: admin
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #         show_sql: true
        format_sql: true
logging:
  level:
    org.hibernate.SQL: debug
    # org.hibernate.type: trace